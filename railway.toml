[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm nx run @org/api:build"

[deploy]
startCommand = "pnpm prisma migrate deploy && node api/dist/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
